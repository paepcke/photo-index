%Image::ExifTool::UserDefined = (
    # Define a new XMP namespace
    'Image::ExifTool::XMP::Main' => {
        # 'dresl' is your namespace prefix (arbitrary, keep it short)
        dresl => {
            SubDirectory => {
                TagTable => 'Image::ExifTool::UserDefined::dresl',
            },
        },
    },
);

# Define the tags within your namespace
%Image::ExifTool::UserDefined::dresl = (
    GROUPS => { 0 => 'XMP', 1 => 'XMP-dresl', 2 => 'Image' },
    NAMESPACE => { 'dresl' => 'http://dresl.org/1.0/' },
    WRITABLE => 'string',
    
    # Your Custom Tags
    IndexUID => { },          # Simple string for your DB ID
    SourceVideoUID => { },    # ID of the parent video
    SourceVideoPath => { },
    SourceFrameNum => { },    # Timestamp of the frame in the video
    LlamaVersion => { },      # Track which model version analyzed this
);